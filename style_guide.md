Indents should be 2 spaces instead of a single tab.

When using the namespace "std," use the statement "using namespace std"
  at the top of the file

When there is a need to indent, use braces - even if it is just a one line statement.

When using braces, put the opening brace on a new line.
