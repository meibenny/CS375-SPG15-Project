This folder contains files to test the sorting programs.

Use the naming convention <input_size><input_state>.txt for unsorted files
Where <input_size> is an integer representing the size of the input
Where <input_state> is one of sorted, r-sorted, or randomized
